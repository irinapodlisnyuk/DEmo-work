import webpack from 'webpack';
export const applyEntryPlugin = (compiler, context, entries, name = 'main') => {
    entries.forEach((entry) => {
        const plugin = new webpack.EntryPlugin(context, entry, name);
        plugin.apply(compiler);
    });
};

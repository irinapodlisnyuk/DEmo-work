import { PartialDeep } from 'type-fest';
import { Options, OptionsWithStyles } from '../types.js';
export declare const formatOptions: (options: PartialDeep<Options>) => Options;
export declare const isOptionsWithStyles: (options: Options) => options is OptionsWithStyles;
